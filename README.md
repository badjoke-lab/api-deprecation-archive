# API Deprecation Archive

API Deprecation Archive is an evidence-based registry for developer-facing API lifecycle changes, including deprecations, removals, sunsets, migration deadlines, replacement paths, and breaking developer-platform changes.

The project is static-first and uses an `entity / event / evidence` data model.

## Status

Initial repository bootstrap in progress.
