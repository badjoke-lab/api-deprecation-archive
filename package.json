{
  "name": "api-deprecation-archive",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "generate:machine": "node scripts/generate-machine-data.mjs",
    "validate:machine": "node scripts/generate-machine-data.mjs --check",
    "validate": "npm run validate:data && npm run validate:machine",
    "test": "npm run validate"
  }
}
